# Supabase (browser — use the anon key only, never the service role key)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=your-anon-key

# Backend API (leave empty in dev to use Vite proxy to localhost:8000)
# VITE_API_URL=https://your-api.example.com
